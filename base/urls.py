# -*- coding: utf-8 -*-
from django.conf.urls import url
from django.views.decorators.csrf import csrf_exempt

urlpatterns = [
#    url(r'^registro/$', views.DoacaoView.as_view(), name="registro"),
#    url(r'^registro/(?P<pk>\d+)/$', views.DoacaoView.as_view(), name="registro"),
]
